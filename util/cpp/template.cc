#include <bits/stdc++.h>
using namespace std;
using Long = long long;
using ii = pair<int, int>;
using ll = pair<Long, Long>;

#ifdef LOCAL
#include <debug.hh>
#else
#define dbg(...) ;
#endif

int main() {
  ios_base::sync_with_stdio(0);
}