#include <bits/stdc++.h>
using namespace std;
// START {{{
#ifdef LOCAL
#include <debug.hh>
#else
#define dbg(...) ;
#endif
#define long int64_t
void solution();
int main() {
  ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  cout << fixed << setprecision(11);
  return solution(), 0;
} // END }}}

void solution() {

}
